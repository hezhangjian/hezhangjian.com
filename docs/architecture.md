# Architecture

The website uses Astro and TypeScript, with dependencies managed by pnpm, and builds as a static site.

Content and attachments are read from a locally synced OneDrive directory, without maintaining content copies in the repository.

The repository contains pages, components, styles, and content loading logic.

See [Content](content.md) for the website's content sections.
